# Local MathQA task

This lm-eval-harness task evaluates the official MathQA `test.json` archive
without using the unavailable legacy Hugging Face dataset loading script.
Its prompt and choice parsing match lm-eval-harness's standard `mathqa` task.
